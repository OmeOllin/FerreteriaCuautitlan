/* Open the sidenav */
function openNav(sidenavName) {
    document.getElementById(sidenavName).style.width = "100%";
}

/* Close/hide the sidenav */
function closeNav(sidenavName) {
    document.getElementById(sidenavName).style.width = "0";
}